
import React from 'react';
import PageHeader from '../PageHeader';

const PageHeaderWrapper = ({ title,id,type }) => (
  <>
    <PageHeader title={title} id={id} type={type}/>
  </>
);

export default PageHeaderWrapper
